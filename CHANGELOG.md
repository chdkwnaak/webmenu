# 변경 기록

이 문서는 `integrated_field_forms.html`의 안정 버전 변경 이력을 기록합니다.

## v1.2.9 - 2026-08-04

- 시추기록부 HWPX의 `현장` 병합 셀 상단과 날씨·지형·경사도 코드 사이에 빈 줄 3개씩 추가
- `S`, `E`, `FT` 등의 현장 코드가 셀 전체 높이에 여유 있게 세로 배치되도록 출력 배열 보강

## v1.2.8 - 2026-08-04

- 시추기록부 HWPX의 병합된 `현장` 셀에서 텍스트가 없는 여백 문단을 복제해 코드가 누락되던 오류 수정
- 현장 날씨·지형·경사도 코드를 `S`, `E`, `FT`와 같이 한 줄씩 세로 출력하도록 HWPX 셀 작성 로직 보강
- 내장 HWPX 템플릿의 특수 문단 구조와 실제 텍스트 문단 선택 여부를 확인하는 회귀 테스트 추가

## v1.2.7 - 2026-08-04

- ASP·CONC 포장 Remark의 깊이를 첫 시료 행 `Depth` 시작값에서 자동으로 가져오도록 연동
- COC 깊이 변경·재연동 시 자동 관리 중인 포장 Remark를 `아스콘 0.15m`, `콘크리트 0.15m` 형식으로 함께 갱신
- 사용자가 Remark를 직접 수정한 경우에는 기존과 같이 자동 갱신 대상에서 제외

## v1.2.6 - 2026-08-04

- 토양표면상태가 ASP 또는 CONC이면 각 현장기록지 첫 행 Remark에 `아스콘  m` 또는 `콘크리트  m`을 자동 입력
- 사용자가 자동 문구의 깊이 또는 내용을 직접 수정한 뒤에는 표면상태 변경·COC 재연동이 해당 값을 덮어쓰지 않도록 보호
- 시추기록부 HWPX에 표면상태 Remark를 반영하고, 단일 시료층에서는 표면상태 문구와 종료사유를 함께 보존

## v1.2.5 - 2026-08-04

- 토양 현장기록지의 `COC순 / 지점번호순` 페이지 정렬을 화면뿐 아니라 문서 인쇄와 시추기록부 HWPX에도 동일하게 적용
- HWPX 지점 묶음과 10개 지점 단위 파일 분할을 선택한 페이지 정렬 기준에 따라 수행
- 내부 COC 연동 데이터 배열은 재배치하지 않아 연동·잠금·백업의 안정성을 유지

## v1.2.4 - 2026-08-04

- 토양 현장기록지 페이지의 보기 순서를 `COC순`과 `지점번호순`으로 전환하는 기능 추가
- 보기 정렬은 원본 페이지 배열, COC 연동, HWPX 내보내기 및 백업 데이터 순서를 변경하지 않도록 분리
- 정렬된 화면에서 페이지 번호 이동·선택·검색이 실제 원본 페이지와 정확히 연결되도록 보강

## v1.2.3 - 2026-08-04

- 시추기록부 HWPX의 항목명을 `채취위치`로 복원하고 값에 이격거리 공란과 계산된 채취깊이를 함께 표시
- 토양 현장기록지의 현장날씨·지형·경사도를 코드로 변환하여 시추기록부 `현장` 칸에 한 줄씩 세로 출력

## v1.2.2 - 2026-08-04

- 시추기록부 HWPX의 `채취위치` 항목을 `채취깊이:`로 변경하고 빈 체크박스·이격거리 문구 제거
- 채취일시 날짜를 `YYYY.MM.DD` 형식으로 출력하고 시간 범위 전체를 괄호로 표시
- 분석항목을 한 줄에 배치하고 쉼표와 공백으로 구분하도록 변경

## v1.2.1 - 2026-08-04

- 토양 현장기록지의 토성·입도, 색상, 수분, 냄새를 1번 시료 기준으로 아래 시료에 일괄 적용하는 기능 추가
- Sample ID가 입력된 행만 적용 대상으로 제한하고 기준값·대상 행·페이지 잠금을 사전 검증하도록 보강
- 이미 같은 값인 행은 유지하며 숨은 일괄 삭제 동작을 제거해 오조작 방지
- 일괄 적용 버튼의 인쇄 제외, 키보드 포커스 및 접근성 안내 추가

## v1.2.0 - 2026-07-31

- 토양 시료채취 현장기록지 데이터를 `예시.hwpx` 형식의 시추기록부로 내보내는 기능 추가
- 내보내기 전 지점·시료층·생성 페이지·파일 수와 입력 오류를 검토하는 화면 추가
- 지점당 시료층을 7개씩 나누고, 이후 페이지의 지점번호에 `(계속)`을 붙여 생성
- 기본 10개 지점 단위로 HWPX를 분할하며 여러 파일은 ZIP으로 묶어 다운로드
- 입도·색상·수분·냄새·심도·층후·종료사유를 확정된 출력 규칙으로 변환
- 예시 양식의 사진은 제거하고 사진 틀만 유지하여 한글에서 사용자가 직접 삽입하도록 구성

## v1.1.4 - 2026-07-31

- COC 도움말 근거 문구에서 불필요한 출처 머리말과 끝 표현을 삭제

## v1.1.3 - 2026-07-31

COC Container Type 도움말을 작은 화면에서도 빠르게 확인할 수 있도록 압축했습니다.

- 표의 셀 여백, 글자 크기와 행간 축소
- 중금속류와 BTEX 세부 항목을 짧은 묶음으로 정리
- 용기 코드, 복수 항목 및 자동 분류 안내를 두 줄로 통합
- 근거 정보를 한 줄형 참고 영역으로 정돈

## v1.1.2 - 2026-07-31

COC 시료 채취·보관 도움말을 현장 확인에 적합한 표로 정돈했습니다.

- 기존 분석항목·용기와 상세 보관 안내를 3열 표로 통합
- 중금속류와 BTEX의 세부 대상 항목을 구분해 표시
- 시료량, 용기 규격, 밀봉과 냉장 운반 조건을 강조
- 다이옥신과 이화학특성의 자동 용기 분류 안내 유지
- 근거 위치를 간결하게 정리

## v1.1.1 - 2026-07-31

기존 COC Container Type 표를 유지하면서 시료 분취·보관 설명을 보강했습니다.

- 분석군별 시료량과 용기 형태 안내 추가
- 밀봉과 0~4℃ 냉장 운반 기준 안내 추가
- 수분보정용 시료 보관 기준 추가
- ES 07130.c와 토양정밀조사 업무편람의 근거 위치 표시

## v1.1.0 - 2026-07-31

COC Container Type 기준을 도움말과 자동 입력에서 함께 관리하도록 개선했습니다.

- 도움말에 `분석항목 - 용기` 형식의 Container Type 기준표 추가
- P 용기 기준에 pH 추가
- G 용기 기준에 다이옥신 추가
- 동일한 공통 기준을 COC 용기 자동 입력과 도움말 표에서 함께 사용
- 이화학특성 프로필은 기존과 동일하게 선택 항목이 있으면 P 용기 적용

## v1.0.1 - 2026-07-31

브라우저나 운영체제의 자동 다크모드가 문서 화면에 적용되지 않도록 표시를
보강한 패치 버전입니다.

- 앱의 지원 색상 모드를 라이트 모드로 명시
- 문서 페이지를 항상 흰 배경과 검은 글자로 표시
- 입력창, 선택창, 버튼과 선택 항목의 기본 색상 체계를 라이트 모드로 고정
- 자동 다크모드에서도 입력값과 안내 문구의 대비가 유지되도록 보호

## v1.0.0 - 2026-07-30

첫 번째 안정 버전 기준선입니다.

- 안전교육일지, 토양 시료채취 현장 기록지, COC, 일일 작업일지 통합 관리
- 프로젝트별 로컬 자동 저장, 실행 취소·다시 실행, 페이지 관리 및 잠금
- 공통정보 프리셋, 검색, 인쇄, 확대·축소 및 모바일 화면 지원
- COC와 토양 현장기록지 자동 연동 및 입력값 검증
- COC 사진 인식 결과 검토·반영을 위한 로컬 OCR 연동
- XLSX 결과입력 양식 내보내기
- 이미지 중복 제거와 무결성 검증을 포함한 `.fieldbackup` 백업·복원

### 호환성

- 앱 버전: `1.0.0`
- 작업공간 데이터 스키마: `3`
- `.fieldbackup` 스키마: `2`

앱 버전과 저장 데이터 스키마는 별도로 관리합니다. 이후 앱 기능이 추가되더라도
저장 구조가 호환되면 데이터 스키마 번호는 유지할 수 있습니다.
