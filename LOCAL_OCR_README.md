# 로컬 COC OCR 서버

이 서버는 사진을 외부 서비스로 보내지 않고, 현재 PC의 Ollama vision 모델로
수기 COC를 판독합니다. 서버는 외부 네트워크에 공개되지 않는
`127.0.0.1`에만 연결됩니다.

## 실행

1. Ollama가 실행 중인지 확인합니다.
2. 처음 다른 PC에서 사용할 때만 다음 명령으로 이미지 처리 및 원근 보정 모듈을 설치합니다.

   `python -m pip install -r requirements.txt`

3. `start_local_ocr.cmd`를 더블클릭합니다.
4. 콘솔에 아래 주소가 표시되면 준비된 것입니다.

   `http://127.0.0.1:8765/api/coc-ocr`

5. 다음 중 하나로 문서를 엽니다.

   - 기존처럼 `integrated_field_forms.html`을 직접 엽니다.
   - 브라우저에서 `http://127.0.0.1:8765/`을 엽니다.

6. COC 문서의 `사진 인식`에서 사진을 선택하고 `사진 인식 요청`을 누릅니다.

서버 주소는 화면에 자동으로 `http://127.0.0.1:8765/api/coc-ocr`가
설정됩니다. 기본 로컬 설정에서는 접속 토큰을 입력하지 않습니다.

서버는 촬영된 종이의 외곽을 찾을 수 있으면 원근을 보정한 뒤, 시료 정보
영역과 분석 체크 영역을 10행씩 나누어 총 3회 판독합니다. 첫 인식은
모델을 메모리에 올리므로 수 분 정도 걸릴 수 있습니다. 서버를 종료하려면
콘솔 창에서 `Ctrl+C`를 누릅니다.

## 설정

실행 전에 환경 변수로 설정을 바꿀 수 있습니다.

- `COC_OCR_MODEL`: 사용할 Ollama vision 모델
- `COC_OCR_PORT`: 로컬 서버 포트, 기본값 `8765`
- `COC_OCR_TOKEN`: 선택적인 Bearer 접속 토큰
- `COC_OCR_ALLOWED_ORIGINS`: 허용할 브라우저 출처 목록. 기본값은 현재 로컬 서버와 `file://` 요청입니다.
- `OLLAMA_URL`: Ollama API 주소, 기본값 `http://127.0.0.1:11434`
- `COC_OCR_TIMEOUT`: 모델 응답 제한 시간(초), 기본값 `300`

별도 환경 변수가 없으면 `start_local_ocr.cmd`는
`gemma4:e4b-it-qat` 모델과 `8765` 포트를 사용합니다. 실행 전에 같은
이름의 환경 변수를 설정하면 배치 파일이 해당 값을 덮어쓰지 않습니다.

## 점검

설치 후 다음 명령으로 HTML 구문, 저장 구조의 기본 무결성, OCR 10행 분할을
확인할 수 있습니다.

`python -m unittest -v test_project.py`
